# CPU-only override: removes the NVIDIA GPU requirement and uses CPU torch/onnxruntime.
# Usage: docker-compose -f docker-compose.yml -f docker-compose.nogpu.yml up -d
services:
  backend:
    build:
      args:
        INSTALL_GPU: "false"
    deploy:
      resources:
        reservations:
          devices: []
